# EVM Utils 🛠️

CLI toolkit for Ethereum developers.

## Commands

- `abi-encode` — encode function calls
- `tx-decode` — decode transaction data
- `event-parse` — parse event logs

## Install

```bash
pip install evm-utils
```
